using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Rendering.HighDefinition;

public class UI_Blur_Easing : MonoBehaviour
{
    private Material material;

    private void Start()
    {
        //material = GetComponent<CustomPassVolume>().customPasses[0].
    }
}
